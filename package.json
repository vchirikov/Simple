{
  "version": "1.0.0-beta",
  "description": "Simple",
  "author": "Vladimir Chirikov",
  "scripts": {
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md --same-file --release-count 0",
    "add_changelog": "conventional-changelog -p angular -i CHANGELOG.md --same-file"
  },
  "devDependencies": {
    "commitizen": "^3.0.2",
    "conventional-changelog-cli": "^2.0.5",
    "cz-conventional-changelog": "^2.1.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
